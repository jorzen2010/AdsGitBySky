@model IEnumerable<AdsEntity.AdsSysUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdsSysUserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdsSysPassword)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdsSysUserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdsSysPassword)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdsSysUserId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdsSysUserId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdsSysUserId })
        </td>
    </tr>
}

</table>
