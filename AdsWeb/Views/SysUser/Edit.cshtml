@model AdsEntity.SysUser

@{
    ViewBag.Title = "Edit";
}

<h2>赵征专用-Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>SysUser</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.SysUserId)

        <div class="form-group">
            @Html.LabelFor(model => model.SysUserName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.SysUserName)
                @Html.ValidationMessageFor(model => model.SysUserName)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.SysPassword, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.SysPassword)
                @Html.ValidationMessageFor(model => model.SysPassword)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
